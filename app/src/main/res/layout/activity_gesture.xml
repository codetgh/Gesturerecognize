<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/drawLetterTv"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:gravity="center"
        android:padding="10dp"
        android:src="@drawable/heart"
        android:layout_gravity="center_horizontal"
        android:contentDescription="@string/draw_letter" />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/lightGrey"
        android:orientation="vertical">

        <android.gesture.GestureOverlayView
            android:id="@+id/gesturesOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gestureColor="@color/green"
            android:uncertainGestureColor="@color/lightGrey" />

    </LinearLayout>

    <TextView
        android:id="@+id/textViewPredictedLetter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:textSize="18sp"
        android:gravity="center_horizontal"
        android:hint="@string/hintPredictedLetter"
        android:textColor="@color/black"/>
</LinearLayout>